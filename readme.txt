# Instructions for Running the Application

1. Ensure you are in the correct directory where the requirements.txt file and the Maze_solver.py script are located.

2. Install the required libraries by running the following command in your terminal:

    pip install -r requirements.txt

3. Run the application using Streamlit by executing the following command:

    streamlit run Maze_solver.py